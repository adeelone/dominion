# Contributing

Use small pull requests with tests. Run `make lint`, `make typecheck`, and `make test` before review.

Simulation changes should include projection tests. Grounding changes should include high-magic success, low-magic reinterpretation, immediate consequence, and world-breaking decline coverage.
